#!/usr/bin/env python3
""" Annotated to_str function """


def to_str(n: float) -> str:
    """
    Returns the string representation of the float.
    : n(float) : The float to be converted to a string.
    : return(str) : The string representation of the float.
    """
    return str(n)
